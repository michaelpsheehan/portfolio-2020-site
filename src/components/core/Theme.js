import React from 'react'

const Theme = ({ theme }) => (
  <>
    {/* //{' '} */}
    {/* <div className={`${theme === 'dark' ? 'f-dark-theme' : 'f-light-theme'}`} /> */}
    <div>theme = {theme}</div>
  </>
)

export default Theme
